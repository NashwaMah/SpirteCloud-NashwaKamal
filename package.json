{
  "name": "spritecloud",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test:staging:headless": "cypress run --headless --spec 'cypress/e2e/**/*' --config viewportWidth=1000,viewportHeight=660  --record ",
    "test:staging:ci": " cypress run --headless --browser chrome  --spec  'cypress/e2e/**/*' --config viewportWidth=1280,viewportHeight=800 --record ",
    "test:staging:local": "npx cypress open"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^12.14.0"
  }
}
